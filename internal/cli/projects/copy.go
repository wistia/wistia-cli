// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package projects

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/wistia/wistia-cli/internal/client"
	"github.com/wistia/wistia-cli/internal/flagutil"
	"github.com/wistia/wistia-cli/internal/interactive"
	"github.com/wistia/wistia-cli/internal/output"
	"github.com/wistia/wistia-cli/internal/sdk"
	"github.com/wistia/wistia-cli/internal/sdk/models/operations"
	"github.com/wistia/wistia-cli/internal/usage"
)

var copyCmdMeta = []flagutil.FlagMeta{
	{FlagName: "id", Shorthand: "i", FieldPath: "ID", Kind: flagutil.FlagKindString, Required: true, Description: "Project Hashed ID [required]"},
	{FlagName: "admin-email", Shorthand: "a", FieldPath: "Body.AdminEmail", Kind: flagutil.FlagKindString, Optional: true, Description: "The email address of the account Manager that will be the owner of the new project. Defaults to the Account Owner if invalid or omitted."},
}

// initCopyCmd initializes the copy command.
func initCopyCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "copy",
		Short:   "Project Copy",
		Long:    "This method does not copy the project’s sharing information (i.e. users that could see the old project will not automatically be able to see the new one).\nFor the request you can specify the owner of a new project by passing an optional parameter. The person you specify must be a Manager in the account.\nFor the response, the HTTP Location header will be set to the URL where the new project resource resides. The body of the response will contain an object representing the new copy of the project that was just created.\n\n## Requires api token with one of the following permissions\n```\nRead, update & delete anything\n```",
		Example: "  wistia projects copy --id <id>",
		RunE:    runCopyCmd,
	}
	flagutil.RegisterFlags(cmd, copyCmdMeta)
	if err := flagutil.ValidateMeta[operations.PostProjectsIDCopyRequest](copyCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for copy: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runCopyCmd executes the copy command.
func runCopyCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, copyCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, copyCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.PostProjectsIDCopyRequest](cmd, copyCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Projects.Copy(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
