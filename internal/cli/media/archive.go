// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package media

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/wistia/wistia-cli/internal/client"
	"github.com/wistia/wistia-cli/internal/flagutil"
	"github.com/wistia/wistia-cli/internal/interactive"
	"github.com/wistia/wistia-cli/internal/output"
	"github.com/wistia/wistia-cli/internal/sdk"
	"github.com/wistia/wistia-cli/internal/sdk/models/operations"
	"github.com/wistia/wistia-cli/internal/usage"
)

var archiveCmdMeta = []flagutil.FlagMeta{
	{FlagName: "hashed-ids", FieldPath: "HashedIds", Kind: flagutil.FlagKindStringArray, Required: true, Description: "An array of the media hashed IDs to be archived. [required]"},
}

// initArchiveCmd initializes the archive command.
func initArchiveCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "archive",
		Short:   "Medias Archive",
		Long:    "This method accepts a list of up to 100 medias to archive per request. It processes requests asynchronously and will return a background_job_status object rather than the typical Media response object. Note that Livestream medias and Soapbox videos imported to Wistia before September 1, 2023 cannot be archived.\n\n## Requires api token with one of the following permissions\n```\nRead, update & delete anything\n```",
		Example: "  wistia media archive --hashed-ids '[]'",
		RunE:    runArchiveCmd,
	}
	flagutil.RegisterFlags(cmd, archiveCmdMeta)
	if err := flagutil.ValidateMeta[operations.PutMediasArchiveRequest](archiveCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for archive: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runArchiveCmd executes the archive command.
func runArchiveCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, archiveCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, archiveCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[operations.PutMediasArchiveRequest](cmd, archiveCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Media.Archive(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
