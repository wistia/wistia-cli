// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 177e03827565

package media

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/wistia/wistia-cli/internal/client"
	"github.com/wistia/wistia-cli/internal/flagutil"
	"github.com/wistia/wistia-cli/internal/interactive"
	"github.com/wistia/wistia-cli/internal/output"
	"github.com/wistia/wistia-cli/internal/sdk"
	"github.com/wistia/wistia-cli/internal/sdk/models/operations"
	"github.com/wistia/wistia-cli/internal/usage"
)

var swapCmdMeta = []flagutil.FlagMeta{
	{FlagName: "media-hashed-id", Shorthand: "m", FieldPath: "MediaHashedID", Kind: flagutil.FlagKindString, Required: true, Description: "The hashed ID of the media to be replaced. [required]"},
	{FlagName: "replacement-media-id", Shorthand: "r", FieldPath: "Body.ReplacementMediaID", Kind: flagutil.FlagKindString, Required: true, Description: "The hashed ID of the media that will replace the original media. Must be the same media type as the original. [required]"},
}

// initSwapCmd initializes the swap command.
func initSwapCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "swap",
		Short:   "Swap Media",
		Long:    "Swap one media with another media. This operation queues a background job to replace the original media with the replacement media while preserving the original media's hashed ID and URLs.\n\n## Requires api token with one of the following permissions\n```\nRead, update & delete anything\n```",
		Example: "  wistia media swap --media-hashed-id <id> --replacement-media-id <value>",
		RunE:    runSwapCmd,
	}
	flagutil.RegisterFlags(cmd, swapCmdMeta)
	if err := flagutil.ValidateMeta[operations.PutMediasMediaHashedIDSwapRequest](swapCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for swap: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runSwapCmd executes the swap command.
func runSwapCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, swapCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, swapCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.PutMediasMediaHashedIDSwapRequest](cmd, swapCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Media.Swap(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
