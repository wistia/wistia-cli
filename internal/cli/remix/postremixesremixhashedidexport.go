// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 497cd9455cc7

package remix

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/wistia/wistia-cli/internal/client"
	"github.com/wistia/wistia-cli/internal/flagutil"
	"github.com/wistia/wistia-cli/internal/interactive"
	"github.com/wistia/wistia-cli/internal/output"
	"github.com/wistia/wistia-cli/internal/sdk"
	"github.com/wistia/wistia-cli/internal/sdk/models/operations"
	"github.com/wistia/wistia-cli/internal/usage"
)

var postRemixesRemixHashedIDExportCmdMeta = []flagutil.FlagMeta{
	{FlagName: "remix-hashed-id", Shorthand: "r", FieldPath: "RemixHashedID", Kind: flagutil.FlagKindString, Required: true, Description: "The hashed ID of the remix to export. [required]"},
	{FlagName: "folder-id", Shorthand: "f", FieldPath: "Body.FolderID", Kind: flagutil.FlagKindString, Optional: true, Description: "Hashed ID of the destination folder. Defaults to the source media's folder."},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Optional: true, Description: "Custom name for the exported media."},
}

// initPostRemixesRemixHashedIdExportCmd initializes the post-remixes-remix-hashed-id-export command.
func initPostRemixesRemixHashedIdExportCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "post-remixes-remix-hashed-id-export",
		Short:   "Export Remix",
		Long:    "Export a completed remix to a folder in your account. Triggers the full\nrender pipeline. The remix must have reached \"edit_tree_generated\" status.\n\nIf no folder_id is provided, the remix is exported to the source media's folder.\n\n<!--- HIDE-MCP -->\n## Requires api token with one of the following permissions\n```\nRead, update & delete anything\n```\n<!--- /HIDE-MCP -->",
		Example: "  wistia remix post-remixes-remix-hashed-id-export --remix-hashed-id <id>",
		RunE:    runPostRemixesRemixHashedIdExportCmd,
		Aliases: []string{"prrhie"},
	}
	flagutil.RegisterFlags(cmd, postRemixesRemixHashedIDExportCmdMeta)
	if err := flagutil.ValidateMeta[operations.PostRemixesRemixHashedIDExportRequest](postRemixesRemixHashedIDExportCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for post-remixes-remix-hashed-id-export: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runPostRemixesRemixHashedIdExportCmd executes the post-remixes-remix-hashed-id-export command.
func runPostRemixesRemixHashedIdExportCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, postRemixesRemixHashedIDExportCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, postRemixesRemixHashedIDExportCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.PostRemixesRemixHashedIDExportRequest](cmd, postRemixesRemixHashedIDExportCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Remix.PostRemixesRemixHashedIDExport(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
