// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 18b6707d7792

package remix

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/wistia/wistia-cli/internal/client"
	"github.com/wistia/wistia-cli/internal/flagutil"
	"github.com/wistia/wistia-cli/internal/interactive"
	"github.com/wistia/wistia-cli/internal/output"
	"github.com/wistia/wistia-cli/internal/sdk"
	"github.com/wistia/wistia-cli/internal/sdk/models/operations"
	"github.com/wistia/wistia-cli/internal/usage"
)

var getRemixesRemixHashedIDCmdMeta = []flagutil.FlagMeta{
	{FlagName: "remix-hashed-id", Shorthand: "r", FieldPath: "RemixHashedID", Kind: flagutil.FlagKindString, Required: true, Description: "The hashed ID of the remix. [required]"},
}

// initGetRemixesRemixHashedIdCmd initializes the get-remixes-remix-hashed-id command.
func initGetRemixesRemixHashedIdCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "get-remixes-remix-hashed-id",
		Short:   "Get Remix",
		Long:    "Get the current status of a remix job. When the status reaches\n\"edit_tree_generated\", preview URLs are included in the response.\n\n<!--- HIDE-MCP -->\n## Requires api token with one of the following permissions\n```\nRead all folder and media data\n```\n<!--- /HIDE-MCP -->",
		Example: "  wistia remix get-remixes-remix-hashed-id --remix-hashed-id <id>",
		RunE:    runGetRemixesRemixHashedIdCmd,
		Aliases: []string{"grrhi"},
	}
	flagutil.RegisterFlags(cmd, getRemixesRemixHashedIDCmdMeta)
	if err := flagutil.ValidateMeta[operations.GetRemixesRemixHashedIDRequest](getRemixesRemixHashedIDCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for get-remixes-remix-hashed-id: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runGetRemixesRemixHashedIdCmd executes the get-remixes-remix-hashed-id command.
func runGetRemixesRemixHashedIdCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, getRemixesRemixHashedIDCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, getRemixesRemixHashedIDCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.GetRemixesRemixHashedIDRequest](cmd, getRemixesRemixHashedIDCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Remix.GetRemixesRemixHashedID(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
