// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package explorer

import (
	"github.com/charmbracelet/bubbles/key"
	"github.com/charmbracelet/lipgloss"
)

var (
	accentColor = lipgloss.Color("#38BDF8")
	dimmedColor = lipgloss.Color("#64748B")
	subtleColor = lipgloss.Color("#475569")

	headerTitleStyle = lipgloss.NewStyle().
				Foreground(accentColor).
				Bold(true)

	headerDescStyle = lipgloss.NewStyle().
			Foreground(dimmedColor).
			Italic(true)

	backStyle = lipgloss.NewStyle().
			Foreground(dimmedColor)

	backSelectedStyle = lipgloss.NewStyle().
				Foreground(accentColor).
				Bold(true)

	selectedItemStyle = lipgloss.NewStyle().
				Foreground(accentColor).
				Bold(true)

	normalItemStyle = lipgloss.NewStyle()

	groupItemStyle = lipgloss.NewStyle().
			Bold(true)

	dimmedStyle = lipgloss.NewStyle().
			Foreground(dimmedColor)

	detailTitleStyle = lipgloss.NewStyle().
				Foreground(accentColor).
				Bold(true)

	detailDescStyle = lipgloss.NewStyle().
			Foreground(dimmedColor).
			Italic(true)

	detailSectionStyle = lipgloss.NewStyle().
				Bold(true)

	requiredFlagStyle = lipgloss.NewStyle().
				Foreground(accentColor)

	statusBarStyle = lipgloss.NewStyle().
			BorderTop(true).
			BorderStyle(lipgloss.NormalBorder()).
			BorderForeground(subtleColor).
			PaddingLeft(1).
			PaddingRight(1)

	breadcrumbStyle = lipgloss.NewStyle().
			Foreground(accentColor).
			Bold(true)

	hintStyle = lipgloss.NewStyle().
			Foreground(dimmedColor)

	searchStyle = lipgloss.NewStyle().
			Foreground(accentColor)

	matchHighlightStyle = lipgloss.NewStyle().
				Foreground(accentColor).
				Underline(true)
)

// keyMap defines the key bindings for the explorer.
type keyMap struct {
	Up     key.Binding
	Down   key.Binding
	Enter  key.Binding
	Back   key.Binding
	Search key.Binding
	Quit   key.Binding
}

var keys = keyMap{
	Up: key.NewBinding(
		key.WithKeys("up", "k"),
	),
	Down: key.NewBinding(
		key.WithKeys("down", "j"),
	),
	Enter: key.NewBinding(
		key.WithKeys("enter", "right", "l"),
	),
	Back: key.NewBinding(
		key.WithKeys("esc", "left", "h", "backspace"),
	),
	Search: key.NewBinding(
		key.WithKeys("/"),
	),
	Quit: key.NewBinding(
		key.WithKeys("q", "ctrl+c"),
	),
}
